import styled from "styled-components";

export const Home = styled.section`
    height: 100vh;
    max-width: 100%;
    background: linear-gradient(120deg, rgba(0, 0, 0, 0.945) ,var(--azul));
`

export const Content = styled.div`
    height: 100vh;
    max-width: 800px;
    background: transparent;
    margin: auto;

    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 20px;
    padding: 15px;
    margin: auto;
`